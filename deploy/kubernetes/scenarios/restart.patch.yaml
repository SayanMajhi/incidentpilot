spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/version: v41
    spec:
      containers:
        - name: app
          env:
            - name: MODE
              value: restart
